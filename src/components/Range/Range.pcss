.range {
  flex: 1;
}

input[type='range'].range__input {
  appearance: none;
  display: inline-block;

  width: 100%;
  height: 40px;

  background: transparent;
}

input[type='range'].range__input:focus {
  outline: color(#157cfc a(50%)) 2px solid;
  outline-offset: 1px;
}

input[type='range'].range__input::-webkit-slider-runnable-track {
  appearance: none;
  height: 4px;

  border-radius: 2px;
  border: none;
  background: #9b9b9b;

  cursor: pointer;
}

input[type='range'].range__input::-webkit-slider-thumb {
  appearance: none;
  width: 20px;
  height: 20px;

  margin-top: -8px;

  border: none;
  border-radius: 50%;
  background: #212121;
  box-shadow: 0 3px 5px 0 rgba(0, 0, 0, .3);

  cursor: pointer;

  transition: box-shadow .3s, background .3s;
}

input[type='range'].range__input::-webkit-slider-thumb:hover,
input[type='range'].range__input::-webkit-slider-thumb:focus {
  background-color: #5b5b5b;
  box-shadow: 0 3px 7px 0 rgba(0, 0, 0, .5);
}

input[type='range'].range__input::-webkit-slider-thumb:active {
  background-color: #666;
  box-shadow: 0 3px 7px 0 rgba(0, 0, 0, .5);
}
